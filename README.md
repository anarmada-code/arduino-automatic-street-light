# arduino-automatic-street-light
An Arduino-based automatic street light simulation that turns an LED ON when it is dark and OFF when it is bright. The project uses an LDR sensor to detect light intensity.
